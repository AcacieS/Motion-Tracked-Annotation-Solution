# Motion-Tracked Annotation Solution
# Installation
``` 
conda create -n motion-tracked-proj python=3.12
conda activate motion-tracked-proj

python -m pip install opencv-contrib-python
python -m pip install caer
